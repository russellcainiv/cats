# Linear setup

Status: prepared; repository-scoped sync is waiting for the public GitHub repository to exist.

## Workspace and current integration

- Linear workspace URL: https://linear.app/rciv
- Workspace slug: `rciv`
- Authenticated account: Russell Cain (`russellcaintattoos`)
- GitHub organization connection: `russellcainiv`, enabled in Linear on 2026-09-09; code access is enabled.
- Linear GitHub integration: enabled at https://linear.app/rciv/settings/integrations/github
- Existing GitHub issue-sync repositories visible in the workspace:
  - `russellcainiv/confirmity` → ❤️ CONFIRMITY (`CON`), bidirectional sync (default)
  - `russellcainiv/lvl2` → LVL2 (`LVL`), bidirectional sync (default)
  - `russellcainiv/backrm` → Backrm (`BKRM`), bidirectional sync (default)

The repository was created at https://github.com/russellcainiv/cats (public) on 2026-09-20. Created Linear team **Cats** with identifier `CATS`: https://linear.app/rciv/team/CATS/overview. Created project **Cats Browser Game**: https://linear.app/rciv/project/cats-browser-game-28162bce1f26/overview.

The native mapping is now correctly saved under **Cats (`CATS`)**. Linear's confirmation notification says `russellcainiv/cats` issues will be added to Linear team Cats, and the saved row reads “Issues are synced in both directions (default).” The earlier mistaken Backrm mapping was removed; the existing confirmity, lvl2, and backrm rows remain unchanged.

## Required completion after repository creation

1. The repository mapping is complete and verified in the native settings UI.
4. Confirm the resulting row says issues are synced in both directions (default), then verify one representative issue's title, body, status, and cross-links in both systems.
5. Record the Cats team key/ID, project ID/URL, GitHub repository ID/URL, sync row text, and the exact verification issue links here.

Linear's settings currently show the sync contract as “Keep GitHub and Linear issues in sync, with updates reflected in both tools”; this observation alone is not proof that the future Cats mapping is active. Linked issues and pull-request links are also not evidence of ongoing bidirectional issue synchronization, so the post-creation verification must include an actual mirrored issue update.
