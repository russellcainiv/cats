# Issue tracker

Canonical tracker: public GitHub repository https://github.com/russellcainiv/cats. The user explicitly requested public repository creation, issue publication and Linear synchronization after the planning draft was prepared. Publication is authorized; do not repeat a granularity interview before carrying out that setup.

The spec lives in .scratch/cats/spec.md; each build ticket has one local mirror under .scratch/cats/issues. docs/tracker-map.json records actual GitHub and Linear identifiers after creation. Never infer a tracker ID from the local task number. Keep canonical bodies, acceptance criteria and dependencies aligned.

Use ready-for-agent for specified implementation work, not as a claim that dependencies are complete. Labels cover kind, domain and priority. Do not close issues until the mission and acceptance evidence are complete. Preserve valid labels.

## Wayfinding operations

A wayfinder:map issue indexes decision tickets. Children use native GitHub sub-issue relationships; prerequisites use native issue dependencies. Refer to issues by linked title in user-facing text. Decision tickets remain distinct from implementation tasks and may gate final acceptance without halting independent implementation. Record human decisions as resolution comments, never answer for the user.

## Linear

Scope integration to this repository and its Cats team/project. Native synchronization is preferred to duplicated automation. Record actual field coverage and verify a controlled change round trip; links alone do not establish ongoing synchronization. Never modify another project's mappings to make Cats work.
