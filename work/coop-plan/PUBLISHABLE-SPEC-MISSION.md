# Repair the co-op plan and prepare complete local specification

LATEST STEERING: before finalizing, read ../FAMILY-GENETICS-DECISIONS-02.md. Shared household and separate simultaneous devices are now CONFIRMED, with four cats per player (eight total). Include ownership/reservations, safe assignment, migration and concurrent-capacity coverage in R26 and co-op tasks. Genetics is newly requested; reserve a genetics follow-up task/requirement but do not conflict with a parallel genetics mission. Shared-control and exact genetic probabilities are pending clickable choices. Do not preserve stale 'mode unconfirmed' language.

AGY Gemini3.8FlashHigh documentation worker. Own work/orchestrator/coop-plan/** except coordinator review files; docs/requirements.json, .scratch/cats/spec.md, docs/contracts.md, docs/architecture.md, docs/tickets.json, .scratch/cats/issues/33-* through38-*, docs/superpowers/plans/2026-09-20-cats.md, docs/ticket-index.md, docs/decision-map.md, START-HERE.md, README.md, .gauntlet/bar/COOP-ADDENDUM.md and planning validator count update only. You are not alone: application/server worker21282 and all source/domain/UI owners continue independently. Do NOT edit application/tests/packages, base frozenBAR.md, original task01–32 acceptance criteria, original reviewers or orchestrator progress. Do not mutate GitHub/Linear or Jules yet; main handles publication after independent plan review.

Read COORDINATOR-REVIEW-01.md and fix every substantiated planning defect. R26 is already added to requirements.json andR11 now includes daughter. Mode choices still pending; orchestrator explicitly stated working default same household from separate devices, world paused when neither active. Record as assumption, never confirmed user choice. Preserve all original requirements plus compatible current capabilities.

IMPORTANT temporal correction: your first PLAN said Vercel Functions do not support WebSockets and quoted stale limits without sources. Coordinator verified current official docs/search: https://vercel.com/docs/functions/websockets, https://vercel.com/kb/guide/do-vercel-serverless-functions-support-websocket-connections and https://vercel.com/docs/functions/configuring-functions/duration now describe native WebSocket support/Fluid compute and plan-specific duration. Read current official sources before claiming runtime limits. SSE plus HTTP commands remains a viable deliberate choice but cannot be justified by a false blanket WebSocket prohibition. Do not invent citations or infrastructure facts. Cite direct official source URLs in plan.

Recommend one coherent implementation: PostgreSQL-backed serialized authoritative commands/ticks and durable revision/event replay, with bounded SSE/reconnect and HTTP commands as simple two-player transport unless evidence justifies another concrete choice. Do not let independent clients commit arbitrary replacement snapshots as authority. Share the actual canonical reducer/simulation on the server for authenticated intents; if retaining a client timing leader, it schedules bounded tick requests only, server validates fenced lease/order/idempotency and applies domain state atomically. No in-process global source of truth, duplicate module receipts, independent PRNG/timers or client-trusted financial/capacity changes. Preserve owner-create unique constraint; membership alone does not require removing it.

Define precise authority lease/heartbeat/graceful-handoff/abrupt-expiry semantics and a realistic maximum failover wait. No5s guaranteed failover with30slease, no zero-downtime promise contradicted by pause. No simulation when no active players and no wall-time catch-up; in-flight commands obey server ordering against last committed authoritative revision. Recovery uses last committed state, not an unknowable exact physical lid-close timestamp. Both humans can submit real commands; neither is forcibly spectator. Private invites/member revocation and authenticated actor checks must cover every HTTP/SSE/save/recovery entry point. No external invitations sent.

Write complete additive tasks33–38 from the plan (adjust titles/scope as required for coherent dependency graph), all with at least4 concrete acceptance criteria, deterministic E2E fixtures, commands/routes/state schema/error cases, browser/phone dogfood, independent blind review and no-criteria-weakening clauses. Add corresponding JSON manifest, markdown issue mirrors and plan tasks with tests/e2e/<slug>.spec.ts references. Extend spec with R26 plus complete co-op user stories. Keep task32 original criteria intact but clearly require new co-op acceptance as an additive release gate. Ensure ticket ordering/acyclic dependencies and count consistency. Update planning validator confirmed requirement count25→26 only because user explicitly addedR26; preserve every other check and negative self-test. Original25requirement content remains except privacyR11 explicitly addingdaughter and documented compatible co-op interpretation of pause/save assumptions.

Create GAUNTLET additive criteria in .gauntlet/bar/COOP-ADDENDUM.md, preserving basebar byte-for-byte. Update links/start/readme/ticketindex with new total/count and working assumptions. Run python3 scripts/validate_plan.py all and fix every local planning inconsistency. Scan diff to prove no original acceptance was weakened. Maintain work/orchestrator/coop-plan/PROGRESS.md with exact evidence. Do not commit other people's files or publish trackers; return local plan/doc paths, validation and changed criteria explanation for fresh blind review. Finish autonomously.
