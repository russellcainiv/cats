# Plan private family co-op as a complete extension

You are AGY Gemini3.8FlashHigh planning/research worker. Do NOT write application code or mutate trackers. You are not alone. Own only work/orchestrator/coop-plan/** in this foundation worktree. Read COOP-SCOPE-ADDITION.md, current docs/contracts.md, architecture.md, tickets.json, spec and exact auth/save implementation. User: "I WANT TO ALSO MAKE IT SO HER AND HER DAUGHTER CAN PLAY TOGETHER". Two quick choices are pending: same household vs separate homes in shared town vs both, and simultaneous own devices vs one screen. Prepare a concrete default plan for SAME HOUSEHOLD ON TWO DEVICES, mark that as an assumption, and enumerate only the deltas if she chooses shared-town separate homes/both. Do not falsely record it as confirmed.

Preserve all accepted gameplay,8living cats per household, clothing, permanence, private gift and no unattended offline progression. New explicit co-op scope legitimately changes only incompatible single-player assumptions. Design private family enrollment/membership/invite redemption/revocation and authenticated actor isolation; synchronized authoritative simulation and command ordering, lease/fencing/CAS/idempotency, simultaneous cat/build/money/pregnancy conflict handling; presence/selection feedback; pause/speed semantics; hidden/disconnected/late joining clients; simulation pauses when no authorized active player remains, with no elapsed wall-time catch-up; deterministic rejoin and same saved household across phone/computer. No public matchmaking/chat/stranger access or collection of child age/date-of-birth is requested. Invitations must be prepared as private links, not sent externally without explicit instruction.

Examine current Next/Vercel/Postgres constraints and official current primary docs where transport/runtime assumptions can change. Propose ONE coherent viable production architecture, not a menu. Account for Vercel's process/request lifecycle and serverless socket constraints; don't assume a persistent in-process global timer/server. Keep one canonical committed world and reliable authority failover. Think through shared-room save ownership and recovery privacy. True simultaneous player input must work; a second device read-only spectator is not fulfillment. Keep local same-device mode usable too.

Deliver concise but build-ready PLAN.md, decision/capability/dependency matrix, concrete added task breakdown with full acceptance/tests, migrations/API/events/state/UX contract, failure scenarios and release/physical-device gates. Identify exact existing assumptions that change and those preserved. Existing tasks01–32 remain intact; propose additive task33+ and a newR26 requirement, append gauntlet addendum rather than erasing originalbar. Do not independently edit original criteria/scripts/source or imply approval. Also record any unresolved decision that materially affects implementation.

Use graph-first source discovery. Parent current foundation graph is earlier snapshot and source is changing; reindex/coverage or exact source fallback required. Do not claim graph evidence absent. No broad tests, new modelservers, shared config, browser windows or PRs. Return file paths and one recommended approach with the key tradeoff. Continue autonomously without user questions.
