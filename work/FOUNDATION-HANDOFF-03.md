# Resume after foreground-server tool wait

Read FOUNDATION-HANDOFF-02.md for full ownership and criteria. Prior worker made real auth repairs and launched Next successfully, but its run_command step226 remained RUNNING since17:03:56 while the server served HTTP200. This is a long-lived server/tool wait, not missing auth access. Coordinator verified task-owned Next PID80981 (parent80965), cwd exactly this Cats foundation worktree, port43171, HTTP200. Prior AGY coordinator PID49887 is stopped before this new worker; application server is preserved if it survives CLI shutdown. Do NOT launch another server while43171 responds. Use existing runtime for tests and continue immediately.

Source currently has auth.ts scrypt credentials, updated allowlist/session route, SQL users/request_hash/owner uniqueness work. Preserve it, read tests and fix actual failures. Update progress. Finish original task01 and the following authorized tasks23/24/31 from prior handoff. Never issue a foreground server command that prevents your turn from returning. If the existing task-owned server stops, ask coordinator through progress for a managed restart and continue all independent source/tests; do not stop work over runtime ownership. Main can operate the server directly.

Use shared BrowserOS or installed headless bounded test tooling, not AGY built-in browser driver installation. Ignore optional watcher/worktreeconfig or driver404 diagnostics as tooling limitations, not reasons to rewrite the project. Never print credentials or place them in screenshot/commits. Commit coherent owned increments and provide actual test results.
