# Cats acceptance auditor — independent preparation

Worktree /Users/russell/.codex/worktrees/cats-foundation/Cats. You use AGY Gemini 3.8 Flash High. Read only the frozen .gauntlet/bar/BAR.md, original .scratch/cats/spec.md, docs/requirements.json, docs/tickets.json, ticket Markdown files, docs/contracts.md, architecture, and implementation plan. OPEN the approved art. Do not read builder reports, transcripts or explanations.

Own ONLY work/acceptance/readiness.md and work/acceptance/coverage.json. You are not alone; no edits to app, tests, shared config, git state, bar, or other lane files. Do not launch browsers, tests, models or subagents. No claims of implementation completion.

Produce a bounded acceptance package:
1. Requirement→ticket→observable scenario→evidence-tier matrix for all25 requirements and32 tickets. Explicitly cover phone controls, persistence, owner isolation, autonomy arbitration, Moo-Moo refusal/no roll/capacity/slot races, permanence and no absent time, free-build exploits, real services, actual recipient acceptance.
2. Preflight table: one row each task checking internal consistency of proposed file/code/test contract; another table each shared-interface task pair derived from manifest/module paths showing producer and consumer consistency. Identify real contradictions that would cause implementation failure; don't manufacture critique.
3. Three critic lens briefs: behavior/security; visual/UX; whole-game/regressions. Judge full original criteria, 100/100 only with direct evidence, no builder rationale. Generate exact artifacts the orchestrator must capture independently; deterministic duplicate-capture protocol before judging.
4. Document which final gates require actual recipient/physical-device information and which implementation can advance safely first. Don't invent identities or mark human acceptance from agents.

Current known ruling: openScenario's detailed {householdId:string} return takes precedence over later stale Promise<void> line. Treat approved image as direction (no pixels-exact match to dynamic game), while duplicate implementation captures must themselves be deterministic. Shared state/schema/render are coupled, so only one implementation owner until contracts stabilize.

Keep report concise but complete; write facts and concrete tests, not generic advice. Return report paths and any critical unresolved contract contradictions. No external side effects.
