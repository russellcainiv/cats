# Cats — AGY Gemini 3.8 Flash engine owner

The user authorized the full game build, maximum useful parallelism, AGY Gemini 3.8 Flash High plus Jules, and lightweight Git. You implement; Codex orchestrates and independently judges. Work now, do not ask whether to proceed. You are not alone: never revert another worker or edit outside your owned paths.

Exact worktree: /Users/russell/.codex/worktrees/cats-engine/Cats. Branch codex/feat-game-engine-20260920. Start by reading this exact file, not searching for it.

Read AGENTS.md, .scratch/cats/spec.md, docs/requirements.json, docs/architecture.md, docs/contracts.md, docs/tickets.json, and relevant plan steps in docs/superpowers/plans/2026-09-20-cats.md. All original criteria remain required. This is a real playable Sims-like cat household, not a clicker or static mock.

OWN: src/domain/state.ts, commands.ts, simulation.ts, selectors.ts, invariants.ts, rng.ts, scenarios.ts; src/domain/core/**; tests/domain/core/**; work/engine/**. Own initial canonical types and starter world. Implement deterministic fixed-step simulation, typed atomic/idempotent commands, bounded event history, seeded RNG state, honest selectors, invariants, no absent-time advance, cat creation and appearance traits, needs/care/autonomy/personality utility and pathfinding/action queues. All other subsystems plug into this core. You may create a temporary LOCAL test harness outside the repository if the foundation package is not yet ready; do not edit repo package/config/lock files.

PEERS: foundation integration in ../cats-foundation/Cats owns package/config, app/server/auth/save routes. World UI peer in ../cats-world/Cats owns rendering and DOM HUD. Jules will own isolated src/domain/building/**, social/**, economy/**, neighborhood/**, lifecycle/**, with matching tests and content scoped to its subsystem. Do not implement those subsystem files yourself. Define the WorldState schema and integration interfaces immediately, then write work/engine/CONTRACT.md so coordinator can pass exact types to peers. Prefer module-local state with typed reducers and a central atomic dispatch/advance pipeline. Every module must be reachable from real GameCommand and GameView, never dead modules.

Build core types to cover the full game: stable IDs, household, cats (traits/needs/mood/appearance/location/skills/age/lifeStatus), lots and objects, relationships, pregnancies with capacity reservations, family, wallet and earned/free provenance, inventory, careers, businesses, tutorial/goals, deaths/memorials, simulation clock, RNG, command receipts. Capacity living + reserved <=8. Death permanent, ghosts memorial projections. Moo-Moo readiness is mutual, adults, willing/aligned/in mood. Chance kittens only eligible completion with capacity; no decline RNG or replay redraw. Fixed 150 sim-day age default, no adjustable lifespan. Phone/computer exact state parity. No runtime model calls.

Before substantial implementation publish your exact shared type/export contract in work/engine/CONTRACT.md. Keep work/engine/PROGRESS.md current with actual commands and evidence. Read official docs if uncertain. Use focused node/Vitest behavior tests with negative cases and invariants. Never silently remove criteria or manufacture passing projections. Commit coherent owned work and report commit(s), interface path, test evidence, and next integration action. Do not merge or close issues; coordinator integrates and checks full vertical workflows. Do not wait on reviewers or user feedback. Preserve actual evidence; no secrets in output.
