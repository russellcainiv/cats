# Foundation continuation: real private access, persistence and runtime

You are a fresh AGY Gemini 3.8 Flash High worker continuing existing WIP. Own application scaffold/package/config/lockfiles, src/app/**, src/server/**, src/features/access/** and saves/**, tests/integration/**, relevant foundation E2E tests, work/issue-2/**. You are not alone; do not overwrite or edit other worktree workers, src/domain/**, world/UI/creator/onboarding/styles/art or coordinator/critic docs. Current foundation branch has planning docs through9f0e394 plus untracked source/config/tests; preserve all WIP. The prior resumed agent last changed schema.sql and inspected the users table, then no source/step progress for over15minutes; its exact owned process was stopped before this handoff. Do not try to resume that conversation or wait for it.

Read work/orchestrator/task-01-mission.md, FOUNDATION-REVIEW-01.md, parallel-contract.md, .scratch/cats/issues/01-private-household.md, docs/contracts.md, docs/architecture.md, docs/tickets.json and work/issue-2/PROGRESS.md. The latter is stale and must be updated immediately with actual evidence, custody, remaining gates and next action. Full acceptance is unchanged. No fake identity, no demo auth shortcut, no stub successful saves.

Critical first repair: src/app/api/auth/session/route.ts accepted body.userId (with production fallback test-owner-01) and minted a signed session without verifying credentials. Implement real credential-backed private allowlist identity, securely hashed passwords or a real provider, with no test identity fallback in production. Provision only synthetic task-owned local test users safely; never put secrets in output, screenshots, commits or browser fields. Verify missing/wrong credentials, unallowlisted and cross-owner access fail and valid credentials succeed. Tests/scenarios must use the same auth/session mechanism. Existing local PostgreSQL cats_dev/cats_test and schema.sql may already have users table; inspect before further migration, do not destroy shared data or change shared PostgreSQL service/auth settings. Restrict work to Cats databases.

Complete original create/resume household flow, true SQL persistence, same-command retries, changed-payload idempotency rejection, one-household owner race handling, initial durable snapshot/checksum/version, transaction rollback and auth privacy, plus all original task01 criteria. Existing Next15/React19 scaffold and node_modules are present. Repair any unsupported lint/config tooling in this task-owned package and use focused validation. Do not burn time re-reading package docs once local commands work. Run a usable dev server on assigned43171 as a managed task-owned process; write its actual PID and how to stop it to progress. No fire-and-forget acceptance claims.

Other modules will integrate next. Your provisional src/domain files must not become a competing game architecture; canonical engine lives at /Users/russell/.codex/worktrees/cats-engine/Cats/src/domain and will be imported by coordinator after repairs. World UI lives at /Users/russell/.codex/worktrees/cats-world/Cats and is also being repaired. Keep authentication/persistence seams typed so real core/UI can mount.

After task01 gates, continue within the SAME ownership into original tasks23,24,31: cloud CAS snapshots/revisions, single-writer30s lease with fencing, request idempotency, backup/recovery/export/import, IndexedDB durability and storage errors, hidden/offline pause contract, production schema/migrations/setup documentation and safe real environment provisioning. Read each exact task before work. Prepare cross-device tests using two browser contexts against the real server/database; tests cannot count mocked network. Keep physical-device and recipient acceptance honestly pending where unavailable. Never bypass identity, lower criteria, close original issues or claim whole game done.

Use the existing shared BrowserOS endpoint for UI verification, not new browser bridges or visible Playwright windows. If built-in AGY browser install fails (known Playwright driver404), use shared service; do not stall on that optional provider tool. Bounded headless test tooling already installed is available where it does not open windows. Every screenshot must be opened and inspected, and before/after desktop/mobile evidence is required for final UI PR. Main coordinates final PR/integration.

Make coherent source commits as you complete increments; stage only your owned paths after scanning for secrets. Return exact SHAs and actual focused test counts, not invented results. Keep progressing through safe repairs without questions. If an external account credential is truly missing, finish local provider-backed behavior and every independent gate, then document exact missing prerequisite. Never type secrets into browser fields or output them.
