# Confirmed family co-op and kitten genetics update

User decisions, 2026-09-20, newer than COOP-SCOPE-ADDITION.md and the first co-op planning mission:

- Shared household is CONFIRMED (user answered option 1).
- Two simultaneous players, each on her own phone or computer, is CONFIRMED.
- User: "they can both have up to 4 cats each". Preserve eight total living cats plus pregnancy reservations; add a hard four living/reserved cat slots per player. Every living cat and reserved kitten slot needs a stable family-member allocation. No overflow, silent reassignment, eviction, or deletion on join/leave/revoke/disconnect. Household membership changes must preserve cats, family history, and saved state. Define safe explicit reassignment between the same two family members and migrate existing saves without loss.
- User: "MAKE IT SO THAT IF CATS MAKE BABIES THE OFFSPRING HAS % CHANCES ON GETTING THEIR GENETRIC TRAITS OF THE PARENTS, AND % OF GETTING THE LOOKS". Percentage-based personality/trait and appearance inheritance is required, separately rolled for each inherited feature from both parents. Existing real-cat customization remains. This is a game inheritance model, not a claim of biological accuracy.

Two OPTIONAL choices have just been asked with the clickable question tool. Do not call them answered yet:
1. Whether both players can direct/care for all cats (working recommendation) or each controls only her own.
2. Whether each feature uses 45% parent A, 45% parent B, 10% valid new feature (working recommendation) or 50/50 parents only.

Until replies arrive, document those two as explicit implementation defaults, never as confirmed user answers. Continue independent work. Odds must be centralized and sum to 100%, use the canonical deterministic RNG, and be displayed clearly in the kitten/family flow. Save parentage, RNG result and provenance so reload, retry, cross-device actions and server handoff cannot reroll a kitten. If both parents share a feature, aggregate identical-result probabilities correctly. Exclude clothes, temporary mood/status, acquired skills, career, money and equipment from genetic appearance/trait inheritance. Inherit base coat/pattern/eyes/breed/body features through supported catalog values, not career outfits. Mutually incompatible/duplicate traits must be handled by a documented probability rule rather than unbounded rerolls.

Co-op planning worker owns incorporating these decisions into R26 and relevant co-op tasks. A separate genetics planning/implementation mission will add the genetics requirement and task without weakening original criteria. Coordinate document ownership before touching shared manifests. Original frozen gauntlet bar remains; additions require matching acceptance and proof.
